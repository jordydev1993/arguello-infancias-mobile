# 03 · Diseño

➡️ El **design system** canónico está en **[`/skills/design.md`](../../skills/design.md)**:
colores (tema Argüello azul + púrpura), tipografía Poppins, escala de espaciado,
componentes reutilizables (`PrimaryButton`, `ResidentCard`, …) y reglas WCAG AA.

Se movió a `skills/` porque `AGENTS.md §[8]` obliga a leerlo desde ahí. No hay copia acá.

Referencias vivas del design system en el repo:
- `design-tokens.json`
- `tailwind.config.js`
- `src/components/ui/`
